<div class='container'>
  <div class='row user_main'>
<br><br>
  <h2><%= @employer.name %></h2>
  <h4><%= @employer.email %></h4>
</div>
</div>
<% if current_employer?(@employer) %>
<%= link_to "Edit", edit_employer_path(@employer) %> 
<% end %>
|
<% if current_employer_admin? %> 
<%= link_to 'Delete Account', employer_path(@employer), method: :delete, data: {confirm: "Are You Sure?"} %>
<% end %>